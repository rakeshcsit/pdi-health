<h1>Listing instructors</h1>

<%= link_to 'New Instructor', new_instructor_path %>

<br><br>

<p>We prioritize tier 2 before tier 1 and then tier 3.</p>

<p>For tier 2 instructors there is no order. In the future we will order by experience of instructors and student success in market.</p>

<p>We will only focus on tier 1s that haven’t been given an analysis for more than 14 days.</p>

<p>We will only focus on tier 3s that haven’t been given an analysis for more than 30 days.</p>

<table class="table">
  <thead>
    <tr>
      <th> Active </th>
      <th>Tier</th>
      <th>University</th>
      <th>Instructor</th>
      <th>Audits</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Class code</th>
      <th>Hired date</th>
      <th>Start date</th>
      <th>Pre quiz taken</th>
    </tr>
  </thead>

  <tbody>
    <% @instructor_ids.each do |id| %>
      <% instructor = Instructor.find(id['id'].to_i) %>
      <tr>
        <td><%= instructor.active ? "yes" : "no" %> </td>
        <td><%= link_to instructor.tier_id, instructor.tier %></td>
        <td><%= link_to instructor.university.university, instructor.university %></td>
        <td><%= link_to instructor.instructor, instructor %> (<%= link_to 'Edit', edit_instructor_path(instructor) %>)</td>
        <td>

          <% instructor.audits.each do |a| %>
            <%= link_to a.audit_date, a %>
          <% end %>

        </td>
        <td><%= instructor.email %></td>
        <td><%= instructor.phone %></td>
        <td><%= instructor.class_code %></td>
        <td><%= instructor.hired_date %></td>
        <td><%= instructor.start_date %></td>
        <td><%= instructor.pre_quiz_taken %></td>
      </tr>
    <% end %>
  </tbody>
</table>
