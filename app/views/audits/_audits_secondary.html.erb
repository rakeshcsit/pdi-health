<table class="table">
  <%= render '/audits/audits_secondary_header', report: false %>

  <tbody>
    <% audits.each do |audit| %>
      <%= render '/audits/audits_secondary_row', audit: audit, report: false %>
    <% end %>
  </tbody>
</table>
